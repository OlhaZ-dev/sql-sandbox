# SQL Sandbox

Basic SQL practice exercises focusing on data querying.  
Includes exercises with `SELECT`, `CASE`, and `ORDER BY`.

## Technologies Used
- SQL

## Project Structure
- `README.md` — project overview (this file)  
- `queries/` — `.sql` files with practice queries 
- `results/` — markdown files with query outputs
- `schema/` — table schema and sample data
- `LICENSE` — MIT license


## How to Use
Clone the repository:
   ```bash
   git clone https://github.com/OlhaZ-dev/sql-sandbox.git
